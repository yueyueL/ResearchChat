import StoreStorage, { StorageKey } from './StoreStorage'
import { paperStorage } from './paperStorage'

const storage = new StoreStorage()

export default storage
export { StorageKey, paperStorage }
